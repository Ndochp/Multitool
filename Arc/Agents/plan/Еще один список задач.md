# Еще один список задач (разобран)

> Этот файл разобран на две части — документацию и список задач подсистемы **GitLab.mirror**.

- **Документация** (блок «Набор фактов» / модель данных) → `../../GitLab.mirror/НаборФактов.md` (и копия в `doc/MultitoolDoc/GitLab.mirror/НаборФактов.md`).
- **Список задач** (строки с тегом `#НадоСделать`) → `../Gitlab.MirrorЗадачиРазработки.md` (задачи **M-DOC-001..003**).

| Код | Что нужно сделать (из `#НадоСделать`) |
|-----|----------------------------------------|
| M-DOC-001 | Описать состав метаданных по данным в файле «Архитектура» |
| M-DOC-002 | Описать, что надо заполнить для начала работы с новым GitLab-сервером |
| M-DOC-003 | Описать функции API и соответствия объектов в 1С и GitLab |
