# Задачи на разработку подсистемы Таймтрекинг

Сводный плоский список всех задач по подсистеме таймтрекинга (без группировки по разделам).
Задачи собраны из файлов `arc` и `doc`.

Подсистема таймтрекинга состоит из двух крупных блоков разработки:
- **Лист учёта рабочего времени** — документ «ЛистУчетаРабочегоВремени» и его формы (семейство `ЛУР-*`);
- **Фасад интеграции** — обработки «ФасадИнтеграции», «ПодключениеGitLab», «ИнтеграцияGitLab» и их методы (семейство `FAS-*`).

Источники, из которых задачи сведены в этот документ:
- `Arc/Agents/plan/ТаймтрекингЗадачиЛистУчета.md` (ЛУР-001..ЛУР-014)
- `Arc/Agents/plan/ТаймтрекингЗадачиФасадаИнтеграции.md` (FAS-001..FAS-014)

---

## Активные задачи (не выполнены)

| Код задачи | Название                                                                       | Предшественники           | Последователи | Примечание                                                                                                                                                                                        | Дата выполнения |
| ---------- | ------------------------------------------------------------------------------ | ------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| ЛУР-000    | Пользователь может отражать день в ЛУРВ                                        | Цель                      |               |                                                                                                                                                                                                   |                 |
| ЛУР-001    | Документ «ЛистУчетаРабочегоВремени» — Создание строки                          | —                         | —             | ⏳ Метод `СоздатьСтроку(Задача)`. Тесты ТЕСТ-ЛУР-001..005. Развёрнуто: [ЛУР-001](#задача-лур-001-документ-листучетарабочеговремени--создание-строки)                                               |                 |
| ЛУР-002    | Документ «ЛистУчетаРабочегоВремени» — Автопродление последней строки           | —                         | —             | ⏳ Метод `АвтопродлитьПоследнююСтроку()`. Тесты ТЕСТ-ЛУР-006..008. Развёрнуто: [ЛУР-002](#задача-лур-002-документ-листучетарабочеговремени--автопродление-последней-строки)                        |                 |
| ЛУР-003    | Документ «ЛистУчетаРабочегоВремени» — Завершение дня                           | —                         | —             | ⏳ Метод `ЗавершитьДень()`. Тесты ТЕСТ-ЛУР-009..011. Развёрнуто: [ЛУР-003](#задача-лур-003-документ-листучетарабочеговремени--завершение-дня)                                                      |                 |
| ЛУР-004    | Документ «ЛистУчетаРабочегоВремени» — Группировка строк для синхронизации      | —                         | ЛУР-005       | ⏳ Метод `СгруппироватьСтрокиДляСинхронизации()`. Тесты ТЕСТ-ЛУР-012..016. Развёрнуто: [ЛУР-004](#задача-лур-004-документ-листучетарабочеговремени--группировка-строк-для-синхронизации)           |                 |
| ЛУР-005    | Документ «ЛистУчетаРабочегоВремени» — Синхронизация с Фасадом (основной поток) | ЛУР-004, FAS-002, FAS-009 | ЛУР-006       | ⏳ Метод `Синхронизировать()`. Тесты ТЕСТ-ЛУР-017..021. Развёрнуто: [ЛУР-005](#задача-лур-005-документ-листучетарабочеговремени--синхронизация-с-фасадом-основной-поток)                           |                 |
| ЛУР-006    | Документ «ЛистУчетаРабочегоВремени» — Синхронизация с ошибкой фасада           | ЛУР-005                   | —             | ⏳ Обработка ошибок при синхронизации. Тесты ТЕСТ-ЛУР-022..024. Развёрнуто: [ЛУР-006](#задача-лур-006-документ-листучетарабочеговремени--синхронизация-с-ошибкой-фасада)                           |                 |
| ЛУР-007    | Документ «ЛистУчетаРабочегоВремени» — Обновление описания через Фасад          | FAS-010                   | —             | ⏳ Метод `ОбновитьОписаниеЗаписи()`. Тесты ТЕСТ-ЛУР-025..026. Развёрнуто: [ЛУР-007](#задача-лур-007-документ-листучетарабочеговремени--обновление-описания-через-фасад)                            |                 |
| ЛУР-008    | Форма документа — Выбор задачи                                                 | FAS-003, FAS-014          | —             | ⏳ Динамический список задач через `ФасадИнтеграции.ПолучитьСписокЗадач()`. Тесты ТЕСТ-ЛУР-027..030. Развёрнуто: [ЛУР-008](#задача-лур-008-форма-документа-листучетарабочеговремени--выбор-задачи) |                 |
| ЛУР-009    | Форма документа — Индикаторы строк синхронизации                               | —                         | —             | ⏳ Цветовая индикация статуса синхронизации строк. Тесты ТЕСТ-ЛУР-031..034. Развёрнуто: [ЛУР-009](#задача-лур-009-форма-документа--индикаторы-строк-синхронизации)                                 |                 |
| ЛУР-010    | Документ «ЛистУчетаРабочегоВремени» — Начало дня                               | —                         | —             | ⏳ Метод `НачатьДень()`. Тесты ТЕСТ-ЛУР-035..037. Развёрнуто: [ЛУР-010](#задача-лур-010-документ-листучетарабочеговремени--начало-дня)                                                             |                 |
| ЛУР-011    | Форма документа — Перемещение строки вверх/вниз                                | —                         | —             | ⏳ Команды `СтрокаВверх` / `СтрокаВниз`. Тесты ТЕСТ-ЛУР-038..041. Развёрнуто: [ЛУР-011](#задача-лур-011-форма-документа--перемещение-строки-вверхвниз)                                             |                 |
| ЛУР-012    | Форма документа — Запоминание прошлых значений при изменении строки            | —                         | —             | ⏳ Поддержка отмены изменений (Ctrl+Z). Тесты ТЕСТ-ЛУР-042..045. Развёрнуто: [ЛУР-012](#задача-лур-012-форма-документа--запоминание-прошлых-значений-при-изменении-строки)                         |                 |
| ЛУР-013    | Форма документа — Команда «Собрать таймлоги на дату»                           | FAS-013, FAS-004          | —             | ⏳ Команда `СобратьТаймлогиНаДату`. Тесты ТЕСТ-ЛУР-046..050. Развёрнуто: [ЛУР-013](#задача-лур-013-форма-документа--команда-собрать-таймлоги-на-дату)                                              |                 |
| ЛУР-014    | Форма документа — Обработчики изменения элементов таблицы отчёта               | —                         | —             | ⏳ Таблица «ОтчетЗаДень». Тесты ТЕСТ-ЛУР-051..054. Развёрнуто: [ЛУР-014](#задача-лур-014-форма-документа--обработчики-изменения-элементов-таблицы-отчёта)                                          |                 |
| FAS-001    | ФасадИнтеграции — Создание сессии                                              | —                         | FAS-002..005  | ⏳ Инициализация сессии фасада. Тесты ТЕСТ-FAS-001..005. Развёрнуто: [FAS-001](#задача-fas-001-фасадинтеграции--создание-сессии)                                                                   |                 |
| FAS-002    | ФасадИнтеграции — Диспетчеризация по типу системы                              | FAS-001                   | FAS-006       | ⏳ Делегирование вызовов обработкам интеграции. Тесты ТЕСТ-FAS-006..010. Развёрнуто: [FAS-002](#задача-fas-002-фасадинтеграции--диспетчеризация-по-типу-системы)                                   |                 |
| FAS-003    | ФасадИнтеграции — ПолучитьСписокЗадач                                          | FAS-001                   | ЛУР-008       | ⏳ Метод `ПолучитьСписокЗадач()` → ТаблицаЗначений. Тесты ТЕСТ-FAS-011..013. Развёрнуто: [FAS-003](#задача-fas-003-фасадинтеграции--получитьсписокзадач)                                           |                 |
| FAS-004    | ФасадИнтеграции — ПолучитьЗаписиОВремени                                       | FAS-001                   | ЛУР-013       | ⏳ Метод `ПолучитьЗаписиОВремени()`. Указан в сводной матрице тестов (ТЕСТ-FAS-014..015); отдельного раздела с описанием в исходнике нет                                                           |                 |
| FAS-005    | ФасадИнтеграции — УдалитьЗаписиОВремениПоДате                                  | FAS-001                   | —             | ⏳ Метод `УдалитьЗаписиОВремениПоДате()`. Указан в сводной матрице тестов (ТЕСТ-FAS-016..017); отдельного раздела с описанием в исходнике нет                                                      |                 |
| FAS-006    | Обработка «ПодключениеGitLab» — Создание и контекст                            | FAS-002                   | FAS-007       | ⏳ Контекст подключения (БазовыйURL, ТокенАутентификации). Тесты ТЕСТ-FAS-018..022. Развёрнуто: [FAS-006](#задача-fas-006-обработка-подключениеgitlab--создание-и-контекст)                        |                 |
| FAS-007    | Обработка «ПодключениеGitLab» — Порождение обработки интеграции                | FAS-006                   | FAS-008       | ⏳ Метод `ПолучитьИнтеграцию()`. Тесты ТЕСТ-FAS-023..025. Развёрнуто: [FAS-007](#задача-fas-007-обработка-подключениеgitlab--порождение-обработки-интеграции)                                      |                 |
| FAS-008    | Обработка «ИнтеграцияGitLab» — Инициализация                                   | FAS-007                   | FAS-009..014  | ⏳ Контекст интеграции (БазовыйURL, Токен). Тесты ТЕСТ-FAS-026..028. Развёрнуто: [FAS-008](#задача-fas-008-обработка-интеграцияgitlab--инициализация)                                              |                 |
| FAS-009    | Обработка «ИнтеграцияGitLab» — ДобавитьЗаписьОВремени                          | FAS-008                   | ЛУР-005       | ⏳ POST time_spent. Тесты ТЕСТ-FAS-029..033. Развёрнуто: [FAS-009](#задача-fas-009-обработка-интеграцияgitlab--добавитьзаписьовремени)                                                             |                 |
| FAS-010    | Обработка «ИнтеграцияGitLab» — ИзменитьЗаписьОВремени                          | FAS-008                   | ЛУР-007       | ⏳ Обновление описания timelog (удаление+создание). Тесты ТЕСТ-FAS-034..037. Развёрнуто: [FAS-010](#задача-fas-010-обработка-интеграцияgitlab--изменитьзаписьовремени)                             |                 |
| FAS-011    | Обработка «ИнтеграцияGitLab» — УдалитьЗаписьОВремени                           | FAS-008                   | —             | ⏳ DELETE timelogs. Тесты ТЕСТ-FAS-038..039. Развёрнуто: [FAS-011](#задача-fas-011-обработка-интеграцияgitlab--удалитьзаписьовремени)                                                              |                 |
| FAS-012    | Обработка «ИнтеграцияGitLab» — УдалитьЗаписиОВремениПоДате                     | FAS-008                   | —             | ⏳ Удаление всех timelogs за дату. Тесты ТЕСТ-FAS-040..042. Развёрнуто: [FAS-012](#задача-fas-012-обработка-интеграцияgitlab--удалитьзаписиовремениподате)                                         |                 |
| FAS-013    | Обработка «ИнтеграцияGitLab» — ПолучитьЗаписиОВремени                          | FAS-008                   | ЛУР-013       | ⏳ GET time_entries за период. Тесты ТЕСТ-FAS-043..044. Развёрнуто: [FAS-013](#задача-fas-013-обработка-интеграцияgitlab--получитьзаписиовремени)                                                  |                 |
| FAS-014    | Обработка «ИнтеграцияGitLab» — ПолучитьЗадачи                                  | FAS-008                   | ЛУР-008       | ⏳ Список задач (Issues) пользователя. Тесты ТЕСТ-FAS-045..046. Развёрнуто: [FAS-014](#задача-fas-014-обработка-интеграцияgitlab--получитьзадачи)                                                  |                 |

---

## Выполненные задачи

| Код задачи | Название | Предшественники | Последователи | Примечание | Дата выполнения |
|------------|----------|-----------------|---------------|------------|-----------------|
| — | Выполненных задач подсистемы таймтрекинга пока нет | — | — | Поскольку документ «ЛистУчетаРабочегоВремени» и обработки Фасада интеграции ещё не реализованы в конфигурации (не присутствуют в `src/cf`), все задачи `ЛУР-*` и `FAS-*` находятся в стадии активных | |

---

## Примечания по объёму

- В список включены задачи самой подсистемы **Таймтрекинг**: семейства `ЛУР-*` (документ «ЛистУчетаРабочегоВремени» и его формы) и `FAS-*` (обработки Фасада интеграции и интеграции с внешними системами).
- Задачи библиотеки интеграции **GitLab.mirror** (`GITL-*`, `GITL-TL-*`, `GQL-*`) не вошли в этот документ — они сведены в `Arc/Agents/plan/Gitlab.MirrorЗадачиРазработки.md`.
- Даты выполнения не указаны: задачи пока не реализованы, точные даты появятся по истории коммитов после выполнения.

### Развёрнутые описания задач

Подробные спецификации с юнит-тестами перенесены ниже (из файлов `ТаймтрекингЗадачиЛистУчета.md` и `ТаймтрекингЗадачиФасадаИнтеграции.md`):
- Задачи **ЛУР-001..ЛУР-014** — [ниже в разделе ЛУР](#развернутые-описания-задач-листучетарабочеговремени);
- Задачи **FAS-001..FAS-014** — [ниже в разделе FAS](#развернутые-описания-задач-фасада-интеграции).

---

# Развёрнутые описания задач

## Развёрнутые описания задач «ЛистУчетаРабочегоВремени»

> Описания перенесены из файла `Arc/Agents/plan/ТаймтрекингЗадачиЛистУчета.md`.

## Задача ЛУР-001: Документ «ЛистУчетаРабочегоВремени» — Создание строки

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `СоздатьСтроку(Задача)`  
**Описание:** Создаёт новую строку табличной части с фиксацией времени окончания предыдущей строки.

### Юнит-тесты

#### ТЕСТ-ЛУР-001: Создание первой строки
- **Дано:** Документ «ЛистУчетаРабочегоВремени» создан, состояние = «Черновик», табличная часть пуста
- **Когда:** Вызван `СоздатьСтроку(ЗадачаПоУмолчанию)`
- **То:** Создана строка с Задача=ЗадачаПоУмолчанию, ВремяНачала=ВремяНачалаДня (09:00), ВремяОкончания=ВремяНачалаДня

#### ТЕСТ-ЛУР-002: Создание второй строки фиксирует первую
- **Дано:** Документ содержит одну строку с ВремяНачала="2024-01-15 09:00", ВремяОкончания="2024-01-15 09:00" (не зафиксирована)
- **Когда:** Вызван `СоздатьСтроку(ЗадачаРабочая)` в "2024-01-15 10:30"
- **То:** Первая строка: ВремяОкончания="2024-01-15 10:30"; вторая строка: ВремяНачала="2024-01-15 10:30", Задача=ЗадачаРабочая

#### ТЕСТ-ЛУР-003: Автоматическое определение ТипСтроки
- **Дано:** Вызван `СоздатьСтроку(ЗадачаGitLab)` где ЗадачаGitlab.ВнешняяСистема.ТипСистемы.ИмеетИнтеграцию = Истина
- **Когда:** Строка создана
- **То:** ТипСтроки = «Рабочая» (синхронизируется)

#### ТЕСТ-ЛУР-004: Автоматическое определение ТипСтроки для обеда
- **Дано:** Вызван `СоздатьСтроку(ЗадачаОбед)` где ЗадачаОбед.ВнешняяСистема.ТипСистемы.ИмеетИнтеграцию = Ложь
- **Когда:** Строка создана
- **То:** ТипСтроки = «Не синхронизируется»

#### ТЕСТ-ЛУР-005: ВремяНачала новой строки = ВремяОкончания предыдущей
- **Дано:** Документ содержит строку с ВремяОкончания="2024-01-15 14:00"
- **Когда:** Вызван `СоздатьСтроку(ЗадачаНовая)`
- **То:** Новая строка получает ВремяНачала="2024-01-15 14:00"

---

## Задача ЛУР-002: Документ «ЛистУчетаРабочегоВремени» — Автопродление последней строки

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `АвтопродлитьПоследнююСтроку()`  
**Описание:** Обновляет ВремяОкончания последней активной строки на текущую дату и время.

### Юнит-тесты

#### ТЕСТ-ЛУР-006: Автопродление последней строки
- **Дано:** Документ в состоянии «Активен», последняя строка с ВремяНачала="2024-01-15 09:00", ВремяОкончания="2024-01-15 09:00"
- **Когда:** Вызван `АвтопродлитьПоследнююСтроку()` в "2024-01-15 09:07" (через 7 минут)
- **То:** ВремяОкончания последней строки = "2024-01-15 09:07"

#### ТЕСТ-ЛУР-007: Автопродление не работает в состоянии «Завершён»
- **Дано:** Документ в состоянии «Завершён», последняя строка с ВремяОкончания="2024-01-15 18:00"
- **Когда:** Вызван `АвтопродлитьПоследнююСтроку()` в "2024-01-15 18:07"
- **То:** ВремяОкончания не изменилось ("2024-01-15 18:00")

#### ТЕСТ-ЛУР-008: Автопродление игнорирует пустой документ
- **Дано:** Документ в состоянии «Активен», табличная часть пуста (нет строк)
- **Когда:** Вызван `АвтопродлитьПоследнююСтроку()`
- **То:** Исключение не генерируется, документ остаётся без изменений

---

## Задача ЛУР-003: Документ «ЛистУчетаРабочегоВремени» — Завершение дня

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `ЗавершитьДень()`  
**Описание:** Фиксирует ВремяОкончания последней строки, переводит документ в состояние «Завершён».

### Юнит-тесты

#### ТЕСТ-ЛУР-009: Завершение дня фиксирует последнюю строку
- **Дано:** Документ в состоянии «Активен», последняя строка с ВремяОкончания="2024-01-15 17:00"
- **Когда:** Вызван `ЗавершитьДень()` в "2024-01-15 18:30"
- **То:** ВремяОкончания последней строки = "2024-01-15 18:30", состояние = «Завершён»

#### ТЕСТ-ЛУР-010: Завершение дня с одной строкой
- **Дано:** Документ в состоянии «Активен», одна строка с ВремяОкончания="2024-01-15 09:00" (не зафиксирована)
- **Когда:** Вызван `ЗавершитьДень()` в "2024-01-15 18:30"
- **То:** ВремяОкончания = "2024-01-15 18:30", состояние = «Завершён»

#### ТЕСТ-ЛУР-011: Повторное завершение дня
- **Дано:** Документ в состоянии «Завершён»
- **Когда:** Вызван `ЗавершитьДень()`
- **То:** Возвращается Ложь, документ не изменяется

---

## Задача ЛУР-004: Документ «ЛистУчетаРабочегоВремени» — Группировка строк для синхронизации

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `СгруппироватьСтрокиДляСинхронизации()` → Массив групп строк  
**Описание:** Группирует последовательные строки с одинаковой задачей для передачи в ФасадИнтеграции.

### Юнит-тесты

#### ТЕСТ-ЛУР-012: Группировка последовательных строк с одной задачей
- **Дано:** Документ содержит 3 последовательные строки с Задача=ЗадачаА, суммарная длительность = 7200 секунд
- **Когда:** Вызван `СгруппироватьСтрокиДляСинхронизации()`
- **То:** Возвращается массив из одной группы: Задача=ЗадачаА, Длительность=7200, Комментарий=""

#### ТЕСТ-ЛУР-013: Разделение групп при смене задачи
- **Дано:** Документ содержит строки: [ЗадачаА(1800с), ЗадачаБ(900с), ЗадачаА(600с)]
- **Когда:** Вызван `СгруппироватьСтрокиДляСинхронизации()`
- **То:** Возвращается массив из 3 групп: [ЗадачаА,1800], [ЗадачаБ,900], [ЗадачаА,600]

#### ТЕСТ-ЛУР-014: Объединение комментариев в группе
- **Дано:** Документ содержит группу строк с комментариями ["Работа над модулем", "Исправление багов"]
- **Когда:** Вызван `СгруппироватьСтрокиДляСинхронизации()`
- **То:** ИтоговыйКомментарий группы = "Работа над модулем; Исправление багов"

#### ТЕСТ-ЛУР-015: Уникализация комментариев
- **Дано:** Документ содержит группу строк с повторяющимися комментариями ["Тест", "Тест", "Отладка"]
- **Когда:** Вызван `СгруппироватьСтрокиДляСинхронизации()`
- **То:** ИтоговыйКомментарий группы = "Тест; Отладка" (уникальные через "; ")

#### ТЕСТ-ЛУР-016: Пустая группа при пустом документе
- **Дано:** Документ в состоянии «Черновик», табличная часть пуста
- **Когда:** Вызван `СгруппироватьСтрокиДляСинхронизации()`
- **То:** Возвращается пустой массив

---

## Задача ЛУР-005: Документ «ЛистУчетаРабочегоВремени» — Синхронизация с Фасадом (основной поток)

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `Синхронизировать()`  
**Описание:** Выполняет синхронизацию строк документа со внешними системами через ФасадИнтеграции.

### Юнит-тесты

#### ТЕСТ-ЛУР-017: Синхронизация группы с пустым ID
- **Дано:** Группа из `СгруппироватьСтрокиДляСинхронизации()`: Задача=ЗадачаGitLab, Длительность=3600, Комментарий="Работа", ИдентификаторЗаписиВремени="" (пустой)
- **Когда:** Вызван `Синхронизировать()`
- **То:** Вызван `ФасадИнтеграции.ДобавитьЗаписьОВремени(ЗадачаGitLab, 3600, ДатаРаботы, "Работа")`, новый ID записан во все строки группы

#### ТЕСТ-ЛУР-018: Пропуск синхронизации при актуальном ID
- **Дано:** Группа с ИдентификаторЗаписиВремени="500" (заполнен, данные не менялись)
- **Когда:** Вызван `Синхронизировать()`
- **То:** ФасадИнтеграции не вызывается, группа пропущена

#### ТЕСТ-ЛУР-019: Пересинхронизация при изменении данных
- **Дано:** Группа с ИдентификаторЗаписиВремени="500", но комментарий в строках изменён на "Новый комментарий"
- **Когда:** Вызван `Синхронизировать()`
- **То:** Вызван `ФасадИнтеграции.УдалитьЗаписьОВремени("500")`, затем `ФасадИнтеграции.ДобавитьЗаписьОВремени(...)`, новый ID записан

#### ТЕСТ-ЛУР-020: Синхронизация нескольких групп
- **Дано:** 2 группы для синхронизации, обе с пустыми ID
- **Когда:** Вызван `Синхронизировать()`
- **То:** Для каждой группы вызваны соответствующие методы фасада, все строки получили новые ID

#### ТЕСТ-ЛУР-021: Пропуск групп без интеграции
- **Дано:** Группа с ЗадачаОбед (ИмеетИнтеграцию = Ложь)
- **Когда:** Вызван `Синхронизировать()`
- **То:** ФасадИнтеграции не вызывается, группа пропущена

---

## Задача ЛУР-006: Документ «ЛистУчетаРабочегоВремени» — Синхронизация с ошибкой фасада

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Описание:** Обработка ошибок при синхронизации через ФасадИнтеграции.

### Юнит-тесты

#### ТЕСТ-ЛУР-022: Обработка ошибки сети при синхронизации
- **Дано:** Группа с пустым ID, ФасадИнтеграции генерирует исключение «Ошибка подключения к GitLab»
- **Когда:** Вызван `Синхронизировать()`
- **То:** Исключение перехвачено, строки группы помечены жёлтым индикатором на форме, идентификаторы не сброшены

#### ТЕСТ-ЛУР-023: Частичная синхронизация при ошибке одной группы
- **Дано:** 3 группы для синхронизации, первая успешна, вторая — ошибка сети, третья — не вызывается
- **Когда:** Вызван `Синхронизировать()`
- **То:** Первая группа синхронизирована (зелёный индикатор), вторая — жёлтый индикатор, третья не обработана

#### ТЕСТ-ЛУР-024: Обработка ошибки аутентификации фасада
- **Дано:** ФасадИнтеграции генерирует исключение «Ошибка аутентификации GitLab: 401 Unauthorized»
- **Когда:** Вызван `Синхронизировать()`
- **То:** Выводится сообщение об ошибке аутентификации, все строки помечены жёлтым индикатором

---

## Задача ЛУР-007: Документ «ЛистУчетаРабочегоВремени» — Обновление описания через Фасад

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `ОбновитьОписаниеЗаписи(ИдентификаторЗаписиВремени, НовыйКомментарий)`  
**Описание:** Обновляет описание записи времени во внешней системе через ФасадИнтеграции.ИзменитьЗаписьОВремени().

### Юнит-тесты

#### ТЕСТ-ЛУР-025: Обновление описания timelog
- **Дано:** Строка с ИдентификаторЗаписиВремени="500", пользователь изменил комментарий на "Новое описание"
- **Когда:** Вызван `ОбновитьОписаниеЗаписи("500", "Новое описание")`
- **То:** Вызван `ФасадИнтеграции.ИзменитьЗаписьОВремени("500", "Новое описание")`, строка обновлена

#### ТЕСТ-ЛУР-026: Обработка ошибки при обновлении описания
- **Дано:** ФасадИнтеграции генерирует исключение «Timelog 500 не найден»
- **Когда:** Вызван `ОбновитьОписаниеЗаписи("500", "Новое описание")`
- **То:** Исключение перехвачено, строка помечена жёлтым индикатором

---

## Задача ЛУР-008: Форма документа «ЛистУчетаРабочегоВремени» — Выбор задачи

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Описание:** Выпадающий список задач, динамически загружается через ФасадИнтеграции.ПолучитьСписокЗадач().

### Юнит-тесты формы

#### ТЕСТ-ЛУР-027: Загрузка списка задач при активации поля
- **Дано:** Форма открыта, пользователь активирует поле выбора задачи
- **Когда:** Активировано поле выбора задачи
- **То:** Вызван `ФасадИнтеграции.ПолучитьСписокЗадач(ТипВнешнейСистемы.Все, ИдентификаторПользователя)`, список задач загружен в выпадающий список

#### ТЕСТ-ЛУР-028: Фильтрация списка задач по подстроке
- **Дано:** Список задач загружен (100 задач), пользователь вводит "Баг"
- **Когда:** Введён текст "Баг" в поле выбора задачи
- **То:** Вызван `ФасадИнтеграции.ПолучитьСписокЗадач(ТипВнешнейСистемы.Все, ИдентификаторПользователя)` с фильтром по подстроке, возвращены только задачи, содержащие "Баг"

#### ТЕСТ-ЛУР-029: Кеширование часто используемых задач
- **Дано:** Пользователь ранее выбирал задачу "Исправить критический баг"
- **Когда:** Активировано поле выбора задачи
- **То:** Задача "Исправить критический баг" отображается в начале списка кешированных задач

#### ТЕСТ-ЛУР-030: Пустой список задач от фасада
- **Дано:** ФасадИнтеграции возвращает пустую ТаблицуЗначений при `ПолучитьСписокЗадач()`
- **Когда:** Активировано поле выбора задачи
- **То:** Выпадающий список пуст, выводится сообщение "Нет доступных задач"

---

## Задача ЛУР-009: Форма документа — Индикаторы строк синхронизации

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Описание:** Цветовая индикация статуса синхронизации каждой строки.

### Юнит-тесты формы

#### ТЕСТ-ЛУР-031: Зелёный индикатор для синхронизированной строки
- **Дано:** Строка с ИдентификаторЗаписиВремени="500", данные не менялись, Задача.ВнешняяСистема.ТипСистемы.ИмеетИнтеграцию = Истина
- **Когда:** Форма отображает строку
- **То:** Строка подсвечена зелёным индикатором

#### ТЕСТ-ЛУР-032: Жёлтый индикатор для строки требующей синхронизации
- **Дано:** Строка с ИдентификаторЗаписиВремени="" (пустой), Задача.ВнешняяСистема.ТипСистемы.ИмеетИнтеграцию = Истина
- **Когда:** Форма отображает строку
- **То:** Строка подсвечена жёлтым индикатором

#### ТЕСТ-ЛУР-033: Серый индикатор для не синхронизируемой строки
- **Дано:** Строка с ЗадачаОбед (ИмеетИнтеграцию = Ложь)
- **Когда:** Форма отображает строку
- **То:** Строка подсвечена серым индикатором

#### ТЕСТ-ЛУР-034: Жёлтый индикатор после изменения комментария
- **Дано:** Строка с ИдентификаторЗаписиВремени="500", пользователь изменил комментарий
- **Когда:** Форма отображает строку
- **То:** Строка подсвечена жёлтым индикатором (требует пересинхронизации)

---

## Задача ЛУР-010: Документ «ЛистУчетаРабочегоВремени» — Начало дня

**Модуль:** `Документ.ЛистУчетаРабочегоВремени.МодульДокумента`  
**Метод:** `НачатьДень()`  
**Описание:** Создаёт документ на текущую дату с одной строкой задачи по умолчанию, переводит в состояние «Активен».

### Юнит-тесты

#### ТЕСТ-ЛУР-035: Создание нового дня
- **Дано:** Документ «ЛистУчетаРабочегоВремени» на "2024-01-15" не существует
- **Когда:** Вызван `НачатьДень()` в "2024-01-15 08:55"
- **То:** Создан документ с Дата="2024-01-15", одна строка с Задача=ЗадачаПоУмолчанию, состояние = «Активен»

#### ТЕСТ-ЛУР-036: Повторное начало дня (документ уже существует)
- **Дано:** Документ на "2024-01-15" существует в состоянии «Черновик»
- **Когда:** Вызван `НачатьДень()`
- **То:** Возвращается Истина, существующий документ переводится в состояние «Активен», строка не дублируется

#### ТЕСТ-ЛУР-037: Начало дня с ЗадачейОбед как первой строкой (если настроено)
- **Дано:** Документ на "2024-01-15" не существует, ЗадачаПоУмолчанию = ЗадачаОбед
- **Когда:** Вызван `НачатьДень()`
- **То:** Создан документ с одной строкой с Задача=ЗадачаОбед, состояние = «Активен»

---

## Задача ЛУР-011: Форма документа — Перемещение строки вверх/вниз

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Команда:** `СтрокаВверх` (и аналогично `СтрокаВниз`)  
**Описание:** Меняет местами текущую строку с предыдущей (или следующей), сохраняя длительность каждой. ВремяНачала и ВремяОкончания обновляются согласно новому порядку строк.

### Юнит-тесты формы

#### ТЕСТ-ЛУР-038: Перемещение второй строки вверх
- **Дано:** Документ содержит 2 строки: Строка1 с ВремяНачала="2024-01-15 09:00", ВремяОкончания="2024-01-15 10:00"; Строка2 (текущая) с ВремяНачала="2024-01-15 10:00", ВремяОкончания="2024-01-15 10:00", Задача=ЗадачаБ
- **Когда:** Выбрана Строка2, выполнена команда `СтрокаВверх`
- **То:** Строки поменялись местами: Строка1 (ЗадачаБ) с ВремяНачала="2024-01-15 09:00", ВремяОкончания="2024-01-15 09:00"; Строка2 (ЗадачаА) с ВремяНачала="2024-01-15 09:00", ВремяОкончания="2024-01-15 10:00"

#### ТЕСТ-ЛУР-039: Перемещение первой строки вверх — отказ
- **Дано:** Выбрана первая (единственная в группе) строка
- **Когда:** Выполнена команда `СтрокаВверх`
- **То:** Строка не перемещена, выводится сообщение "Нельзя переместить выше"

#### ТЕСТ-ЛУР-040: Перемещение с сохранением ИдентификаторЗаписиВремени
- **Дано:** Строка2 с ИдентификаторЗаписиВремени="500" перемещена вверх
- **Когда:** Выполнена команда `СтрокаВверх`
- **То:** ИдентификаторЗаписиВремени="500" остался у той же логической строки (с ЗадачейБ), несмотря на смену порядка в таблице

#### ТЕСТ-ЛУР-041: Перемещение меняет индикаторы строк
- **Дано:** После перемещения строки жёлтый и зелёный индикаторы
- **Когда:** Выполнена команда `СтрокаВверх`
- **То:** Индикаторы привязаны к логическим строкам (по задаче и данным), а не к позициям в таблице — жёлтая строка остаётся жёлтой на новой позиции

---

## Задача ЛУР-012: Форма документа — Запоминание прошлых значений при изменении строки

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Описание:** При изменении реквизита строки (комментарий, задача) форма запоминает предыдущее значение для определения необходимости синхронизации и поддержки отмены изменений (Ctrl+Z).

### Юнит-тесты формы

#### ТЕСТ-ЛУР-042: Запоминание значения комментария при изменении
- **Дано:** Строка с ИдентификаторЗаписиВремени="500", Комментарий="Старый"
- **Когда:** Пользователь изменил комментарий на "Новый" в элементе таблицы
- **То:** Запомнено прошлое значение = "Старый", строка получила жёлтый индикатор

#### ТЕСТ-ЛУР-043: Отмена изменения (Ctrl+Z) восстанавливает прошлое значение
- **Дано:** Строка с запомненным прошлым значением комментария = "Старый"
- **Когда:** Выполнена отмена изменений
- **То:** Комментарий восстановлен в "Старый", жёлтый индикатор снят

#### ТЕСТ-ЛУР-044: Запоминание значения задачи при изменении
- **Дано:** Строка с ИдентификаторЗаписиВремени="500", Задача=ЗадачаА
- **Когда:** Пользователь изменил задачу на ЗадачаБ
- **То:** Запомнено прошлое значение = ЗадачаА, строка получила жёлтый индикатор, требуется пересинхронизация

#### ТЕСТ-ЛУР-045: Изменение не синхронизируемой строки не требует ID
- **Дано:** Строка с ЗадачаОбед (ИмеетИнтеграцию = Ложь), пользователь изменил комментарий
- **Когда:** Комментарий изменён
- **То:** Прошлое значение запомнено, жёлтый индикатор НЕ устанавливается (синхронизация не нужна)

---

## Задача ЛУР-013: Форма документа — Команда "Собрать таймлоги на дату"

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Команда:** `СобратьТаймлогиНаДату`  
**Описание:** Получает все таймлоги пользователя на выбранную дату из всех не архивных систем учета, проводит синхронизацию и формирует строки документа.

### Юнит-тесты формы

#### ТЕСТ-ЛУР-046: Сбор таймлогов из одной системы
- **Дано:** Документ на "2024-01-15" пуст, ФасадИнтеграции.ПолучитьЗаписиОВремениЗаПериод возвращает 2 записи GitLab: [09:00-12:00 "Модуль", 13:00-17:00 "Отладка"]
- **Когда:** Выполнена команда `СобратьТаймлогиНаДату`
- **То:** Созданы 2 строки: Строка1 (Задача=ЗадачаGitLabМодуль, ВремяНачала=09:00, ВремяОкончания=12:00), Строка2 (Задача=ЗадачаGitLabОтладка, ВремяНачала=13:00, ВремяОкончания=17:00), все получили ИдентификаторЗаписиВремени

#### ТЕСТ-ЛУР-047: Синхронизация по ИД таймлога
- **Дано:** В документе есть строка с ИдентификаторЗаписиВремени="500", во внешней системе эта запись изменилась (другой комментарий)
- **Когда:** Выполнена команда `СобратьТаймлогиНаДату`
- **То:** Вызван Фасад для проверки по конкретному ИД, строка обновлена, жёлтый индикатор если данные расходятся

#### ТЕСТ-ЛУР-048: Добавление новых записей в конец нарастающим итогом
- **Дано:** В документе есть одна строка 09:00-10:00 (не синхронизирована), Фасад вернул записи [09:00-12:00, 13:00-17:00]
- **Когда:** Выполнена команда `СобратьТаймлогиНаДату`
- **То:** Первая строка скорректирована до 09:00-10:00 (длительность сохранена), добавлена Строка2 (10:00-12:00), Строка3 (13:00-17:00)

#### ТЕСТ-ЛУР-049: Обработка систем со статусом "серый" (не достучались)
- **Дано:** Фасад вернул записи: GitLab — успешно, Jira — ошибка подключения
- **Когда:** Выполнена команда `СобратьТаймлогиНаДату`
- **То:** Записи из GitLab добавлены с зелёным индикатором, из Jira — с серым (не синхронизированы), ИдентификаторЗаписиВремени для Jira = пустой

#### ТЕСТ-ЛУР-050: Повторный сбор обновляет существующие строки
- **Дано:** В документе есть 3 строки с ИдентификаторыЗаписейВремени, Фасад вернул те же ID но с изменёнными данными для одной записи
- **Когда:** Выполнена команда `СобратьТаймлогиНаДату`
- **То:** Строка с изменившимися данными обновлена и получила жёлтый индикатор, остальные — зелёный

---

## Задача ЛУР-014: Форма документа — Обработчики изменения элементов таблицы отчёта

**Модуль:** `Форма.ЛистУчетаРабочегоВремени`  
**Элемент:** Таблица "ОтчетЗаДень"  
**Описание:** Обработчики событий изменения таблицы обеспечивают корректное обновление времён всех строк при изменении любой ячейки ВремяНачала или ВремяОкончания.

### Юнит-тесты формы

#### ТЕСТ-ЛУР-051: Изменение ВремяНачала строки сдвигает следующую
- **Дано:** 3 строки: [09:00-10:00, 10:00-12:00, 12:00-17:00], пользователь изменил ВремяНачала Строки2 на "2024-01-15 11:00"
- **Когда:** Сработал обработчик `ОтчетЗаДеньНачалоПриИзменении`
- **То:** Строка1: ВремяОкончания="2024-01-15 10:59:59" (за секунду до начала следующей), Строка2: ВремяНачала="11:00", Строка3: ВремяНачала сдвинута на "2024-01-15 12:00" → "2024-01-15 13:00"

#### ТЕСТ-ЛУР-052: Изменение ВремяОкончания текущей строки
- **Дано:** Строка с ВремяНачала="09:00", ВремяОкончания="09:00" (автопродление), пользователь изменил ВремяОкончания на "10:30"
- **Когда:** Сработал обработчик `ОтчетЗаДеньПриИзменении`
- **То:** Длительность строки = 1 час 30 мин, если есть следующая строка — её ВремяНачала сдвинута

#### ТЕСТ-ЛУР-053: Удаление строки пересчитывает таблицу
- **Дано:** 3 строки [09:00-10:00, 10:00-12:00, 12:00-17:00], удалена Строка2
- **Когда:** Сработал обработчик `ОтчетЗаДеньПослеУдаления`
- **То:** Строка1: ВремяОкончания="2024-01-15 09:59:59" (за секунду до Строки3), Строка3: ВремяНачала="10:00", ВремяОкончания="17:00"

#### ТЕСТ-ЛУР-054: Запоминание прошлых значений перед изменением
- **Дано:** Пользователь начал изменение ВремяНачала строки
- **Когда:** Сработал обработчик `ОтчетЗаДеньПередНачаломИзменения`
- **То:** Запомнены исходные значения ВремяНачала и ВремяОкончания текущей строки для поддержки отмены

---

## Развёрнутые описания задач «ФасадИнтеграции»

> Описания перенесены из файла `Arc/Agents/plan/ТаймтрекингЗадачиФасадаИнтеграции.md`.

## Задача FAS-001: ФасадИнтеграции — Создание сессии

**Модуль:** `Обработка.ФасадИнтеграции`  
**Описание:** Создаёт сессию фасада для одного пользователя. Одна сессия обрабатывает все задачи документа синхронизации.

### Юнит-тесты

#### ТЕСТ-FAS-001: Создание сессии фасада
- **Дано:** Новая обработка «ФасадИнтеграции» не инициализирована
- **Когда:** Вызван `ИнициализироватьСессию(ИдентификаторПользователя)`
- **То:** Сессия создана, ИдентификаторПользователя сохранён в контексте сессии

#### ТЕСТ-FAS-002: Повторная инициализация той же сессии
- **Дано:** Сессия фасада уже инициализирована для пользователя 42
- **Когда:** Вызван `ИнициализироватьСессию(42)` повторно
- **То:** Возвращается Истина, контекст не сбрасывается

#### ТЕСТ-FAS-003: Инициализация сессии для другого пользователя
- **Дано:** Сессия фасада инициализирована для пользователя 42
- **Когда:** Вызван `ИнициализироватьСессию(99)` (другой пользователь)
- **То:** Старая сессия завершается, создаётся новая сессия для пользователя 99

#### ТЕСТ-FAS-004: Получение идентификатора пользователя из сессии
- **Дано:** Сессия инициализирована с ИдентификаторПользователя=42
- **Когда:** Вызван `ПолучитьИдентификаторПользователя()`
- **То:** Возвращается 42

#### ТЕСТ-FAS-005: Получение идентификатора пользователя до инициализации
- **Дано:** Сессия не инициализирована
- **Когда:** Вызван `ПолучитьИдентификаторПользователя()`
- **То:** Генерируется исключение «Сессия фасада не инициализирована»

---

## Задача FAS-002: ФасадИнтеграции — Диспетчеризация по типу системы

**Модуль:** `Обработка.ФасадИнтеграции`  
**Методы:** `ДобавитьЗаписьОВремени()`, `ИзменитьЗаписьОВремени()`, `УдалитьЗаписьОВремени()`  
**Описание:** Определяет тип внешней системы по Задача и делегирует вызов соответствующей обработке интеграции.

### Юнит-тесты

#### ТЕСТ-FAS-006: Диспетчеризация для GitLab
- **Дано:** Сессия инициализирована, Задача имеет ВнешняяСистема.ТипСистемы = GitLab
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ЗадачаGitLab, 3600, "2024-01-15", "Описание")`
- **То:** Создана обработка подключения «ПодключениеGitLab» (если не создана), порождена обработка интеграции «ИнтеграцияGitLab», вызван её метод `ДобавитьЗаписьОВремени()`

#### ТЕСТ-FAS-007: Диспетчеризация для Jira
- **Дано:** Сессия инициализирована, Задача имеет ВнешняяСистема.ТипСистемы = Jira
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ЗадачаJira, 3600, "2024-01-15", "Описание")`
- **То:** Создана обработка подключения «ПодключениеJira», порождена обработка интеграции «ИнтеграцияJira», вызван её метод `ДобавитьЗаписьОВремени()`

#### ТЕСТ-FAS-008: Кэширование обработки подключения в сессии
- **Дано:** Вызван `ДобавитьЗаписьОВремени(ЗадачаGitLab, ...)` первый раз — создана обработка «ПодключениеGitLab»
- **Когда:** Вызван `УдалитьЗаписьОВремени(ID)` для той же задачи GitLab во второй раз
- **То:** Используется существующая обработка подключения «ПодключениеGitLab», новая не создаётся

#### ТЕСТ-FAS-009: Обработка задачи без интеграции
- **Дано:** Сессия инициализирована, Задача имеет ВнешняяСистема.ТипСистемы = Внутренняя (ИмеетИнтеграцию = Ложь)
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ЗадачаВнутренняя, 3600, "2024-01-15", "Описание")`
- **То:** Возвращается пустая строка (ID записи не генерируется), интеграция не вызывается

#### ТЕСТ-FAS-010: Обработка неизвестного типа системы
- **Дано:** Сессия инициализирована, Задача имеет неизвестный тип системы
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ЗадачаНеизвестная, 3600, "2024-01-15", "Описание")`
- **То:** Генерируется исключение «Неизвестный тип внешней системы: {Наименование}»

---

## Задача FAS-003: ФасадИнтеграции — ПолучитьСписокЗадач

**Модуль:** `Обработка.ФасадИнтеграции`  
**Метод:** `ПолучитьСписокЗадач(ТипВнешнейСистемы, ИдентификаторПользователя)` → ТаблицаЗначений  
**Описание:** Запрашивает задачи у соответствующей обработки интеграции и объединяет результаты.

### Юнит-тесты

#### ТЕСТ-FAS-011: Получение задач GitLab
- **Дано:** Сессия инициализирована, обработка подключения «ПодключениеGitLab» создана
- **Когда:** Вызван `ПолучитьСписокЗадач(ТипВнешнейСистемы.GitLab, 42)`
- **То:** Обработка интеграции «ИнтеграцияGitLab.ПолучитьЗадачи(42)» возвращает ТаблицуЗначений с задачами

#### ТЕСТ-FAS-012: Объединение задач из нескольких систем
- **Дано:** Сессия инициализирована, созданы обработки подключения для GitLab и Jira
- **Когда:** Вызван `ПолучитьСписокЗадач(ТипВнешнейСистемы.Все, 42)` (тип «Все» = объединить все системы)
- **То:** Возвращается объединённая ТаблицаЗначений с задачами из GitLab и Jira

#### ТЕСТ-FAS-013: Пустой список задач при отсутствии интеграции
- **Дано:** Сессия инициализирована, но обработка подключения «ПодключениеGitLab» не создана (нет настроек)
- **Когда:** Вызван `ПолучитьСписокЗадач(ТипВнешнейСистемы.GitLab, 42)`
- **То:** Возвращается пустая ТаблицаЗначений

---

## Задача FAS-006: Обработка «ПодключениеGitLab» — Создание и контекст

**Модуль:** `Обработка.ПодключениеGitLab`  
**Описание:** Обработка подключения фасада к GitLab. Содержит контекст подключения (БазовыйURL, ТокенАутентификации). Порождает обработку интеграции.

### Юнит-тесты

#### ТЕСТ-FAS-018: Создание обработки подключения GitLab
- **Дано:** ФасадИнтеграции вызывает `СоздатьПодключение(ТипВнешнейСистемы.GitLab)`
- **Когда:** Обработка «ПодключениеGitLab» не существует в кэше сессии
- **То:** Создаётся новая обработка «ПодключениеGitLab», инициализируется данными из справочника «ВнешниеСистемы»

#### ТЕСТ-FAS-019: Получение БазовогоURL из контекста подключения
- **Дано:** Обработка «ПодключениеGitLab» создана с БазовыйURL="https://gitlab.example.com"
- **Когда:** Вызван `ПолучитьБазовыйURL()`
- **То:** Возвращается "https://gitlab.example.com"

#### ТЕСТ-FAS-020: Получение токена из контекста подключения
- **Дано:** Обработка «ПодключениеGitLab» создана с ТокенАутентификации="glpat-abc123"
- **Когда:** Вызван `ПолучитьТокен()`
- **То:** Возвращается "glpat-abc123"

#### ТЕСТ-FAS-021: Проверка валидности токена
- **Дано:** Обработка «ПодключениеGitLab» создана с пустым ТокенАутентификации
- **Когда:** Вызван `ПроверитьТокен()`
- **То:** Возвращается Ложь

#### ТЕСТ-FAS-022: Проверка валидности токена при заполненном токене
- **Дано:** Обработка «ПодключениеGitLab» создана с ТокенАутентификации="glpat-abc123"
- **Когда:** Вызван `ПроверитьТокен()`
- **То:** Возвращается Истина

---

## Задача FAS-007: Обработка «ПодключениеGitLab» — Порождение обработки интеграции

**Модуль:** `Обработка.ПодключениеGitLab`  
**Метод:** `ПолучитьИнтеграцию()` → Обработка  
**Описание:** Создаёт и возвращает обработку интеграции с GitLab, передавая данные подключения и авторизации.

### Юнит-тесты

#### ТЕСТ-FAS-023: Создание обработки интеграции при первом вызове
- **Дано:** Обработка «ПодключениеGitLab» создана, обработка «ИнтеграцияGitLab» не существует
- **Когда:** Вызван `ПолучитьИнтеграцию()`
- **То:** Создаётся новая обработка «ИнтеграцияGitLab», инициализируется БазовыйURL и Токен из контекста подключения

#### ТЕСТ-FAS-024: Кэширование обработки интеграции в контексте подключения
- **Дано:** Вызван `ПолучитьИнтеграцию()` первый раз — создана обработка «ИнтеграцияGitLab»
- **Когда:** Вызван `ПолучитьИнтеграцию()` второй раз
- **То:** Возвращается существующая обработка «ИнтеграцияGitLab», новая не создаётся

#### ТЕСТ-FAS-025: Передача данных подключения в обработку интеграции
- **Дано:** Обработка «ПодключениеGitLab» создана с БазовыйURL="https://gitlab.example.com", Токен="glpat-abc123"
- **Когда:** Вызван `ПолучитьИнтеграцию()`
- **То:** Обработка «ИнтеграцияGitLab» инициализируется с БазовыйURL="https://gitlab.example.com" и Токен="glpat-abc123"

---

## Задача FAS-008: Обработка «ИнтеграцияGitLab» — Инициализация

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Описание:** Обработка интеграции с GitLab. Содержит контекст данных подключения и авторизации. Предоставляет методы работы с GitLab API.

### Юнит-тесты

#### ТЕСТ-FAS-026: Инициализация обработки интеграции URL
- **Дано:** Новая обработка «ИнтеграцияGitLab» не инициализирована
- **Когда:** Вызван `УстановитьБазовыйURL("https://gitlab.example.com")`
- **То:** БазовыйURL сохранён в контексте интеграции

#### ТЕСТ-FAS-027: Инициализация обработки интеграции токеном
- **Дано:** Новая обработка «ИнтеграцияGitLab» не инициализирована
- **Когда:** Вызван `УстановитьТокен("glpat-abc123")`
- **То:** Токен сохранён в контексте интеграции

#### ТЕСТ-FAS-028: Получение базового URL из контекста
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована с БазовыйURL="https://gitlab.example.com"
- **Когда:** Вызван `ПолучитьБазовыйURL()`
- **То:** Возвращается "https://gitlab.example.com"

---

## Задача FAS-009: Обработка «ИнтеграцияGitLab» — ДобавитьЗаписьОВремени

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `ДобавитьЗаписьОВремени(ВнешняяЗадача, ДлительностьВСекундах, ДатаРаботы, Описание)` → ИдентификаторЗаписиВремени  
**Описание:** Создаёт timelog в GitLab через POST /projects/:id/issues/:issue_iid/time_spent.

### Юнит-тесты

#### ТЕСТ-FAS-029: Добавление timelog в GitLab
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает JSON с timelog_id=500 со статусом 201
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ВнешняяЗадача, 3600, "2024-01-15", "Описание")`
- **То:** Возвращается "500" (timelog_id из ответа)

#### ТЕСТ-FAS-030: Формирование URL для POST timelogs
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована с БазовыйURL="https://gitlab.example.com", ВнешняяЗадача имеет ВнешнийИдентификатор="group/proj:42"
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ВнешняяЗадача, 3600, "2024-01-15", "Описание")`
- **То:** POST отправлен на URL "https://gitlab.example.com/api/v4/projects/group%2Fproj/issues/42/time_spent"

#### ТЕСТ-FAS-031: Формирование тела запроса timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает JSON со статусом 201
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ВнешняяЗадача, 3600, "2024-01-15", "Описание")`
- **То:** Тело запроса содержит {"duration": "1h", "spent_at": "2024-01-15T00:00:00Z", "summary": "Описание"}

#### ТЕСТ-FAS-032: Обработка ошибки 401 при добавлении timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 401 со статусом 401
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ВнешняяЗадача, 3600, "2024-01-15", "Описание")`
- **То:** Генерируется исключение «Ошибка аутентификации GitLab: 401 Unauthorized»

#### ТЕСТ-FAS-033: Обработка ошибки 404 при добавлении timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 404 со статусом 404
- **Когда:** Вызван `ДобавитьЗаписьОВремени(ВнешняяЗадача, 3600, "2024-01-15", "Описание")`
- **То:** Генерируется исключение «Задача не найдена в GitLab: 404 Not Found»

---

## Задача FAS-010: Обработка «ИнтеграцияGitLab» — ИзменитьЗаписьОВремени

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `ИзменитьЗаписьОВремени(ИдентификаторЗаписиВремени, Описание)` → ИдентификаторОбновленнойЗаписиВремени  
**Описание:** Обновляет описание timelog. Для GitLab без нативной поддержки — удаление и создание заново.

### Юнит-тесты

#### ТЕСТ-FAS-034: Изменение описания timelog в GitLab (удаление+создание)
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP: DELETE возвращает 204, POST возвращает JSON с новым timelog_id=501 со статусом 201
- **Когда:** Вызван `ИзменитьЗаписьОВремени("500", "Новое описание")`
- **То:** Сначала DELETE /projects/.../timelogs/500, затем POST с новым описанием, возвращается "501"

#### ТЕСТ-FAS-035: Удаление старого timelog перед созданием нового
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 204 при DELETE
- **Когда:** Вызван `ИзменитьЗаписьОВремени("500", "Новое описание")`
- **То:** DELETE отправлен на URL "/api/v4/projects/{id}/timelogs/500"

#### ТЕСТ-FAS-036: Создание нового timelog с обновлённым описанием
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 201 при POST
- **Когда:** Вызван `ИзменитьЗаписьОВремени("500", "Новое описание")`
- **То:** POST отправлен с телом {"summary": "Новое описание"}

#### ТЕСТ-FAS-037: Обработка ошибки при удалении старого timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 404 при DELETE
- **Когда:** Вызван `ИзменитьЗаписьОВремени("999", "Новое описание")`
- **То:** Генерируется исключение «Timelog 999 не найден для обновления»

---

## Задача FAS-011: Обработка «ИнтеграцияGitLab» — УдалитьЗаписьОВремени

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `УдалитьЗаписьОВремени(ИдентификаторЗаписиВремени)`  
**Описание:** Удаляет timelog через DELETE /projects/:id/timelogs/:timelog_id.

### Юнит-тесты

#### ТЕСТ-FAS-038: Удаление timelog из GitLab
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 204 при DELETE
- **Когда:** Вызван `УдалитьЗаписьОВремени("500")`
- **То:** DELETE отправлен на URL "/api/v4/projects/{id}/timelogs/500", возвращается Истина

#### ТЕСТ-FAS-039: Обработка ошибки 404 при удалении timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает 404 при DELETE
- **Когда:** Вызван `УдалитьЗаписьОВремени("999")`
- **То:** Генерируется исключение «Timelog 999 не найден для удаления»

---

## Задача FAS-012: Обработка «ИнтеграцияGitLab» — УдалитьЗаписиОВремениПоДате

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `УдалитьЗаписиОВремениПоДате(ВнешняяЗадача, Дата)` → Число  
**Описание:** Получает все timelogs за дату и удаляет каждый. Возвращает количество удалённых записей.

### Юнит-тесты

#### ТЕСТ-FAS-040: Удаление всех timelogs за дату
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock GET возвращает 2 timelogs за дату, mock DELETE возвращает 204 для каждого
- **Когда:** Вызван `УдалитьЗаписиОВремениПоДате(ВнешняяЗадача, "2024-01-15")`
- **То:** GET получает 2 timelogs, каждый удаляется, возвращается 2 (количество удалённых)

#### ТЕСТ-FAS-041: Удаление при отсутствии timelogs за дату
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock GET возвращает пустой массив
- **Когда:** Вызван `УдалитьЗаписиОВремениПоДате(ВнешняяЗадача, "2024-01-15")`
- **То:** Возвращается 0

#### ТЕСТ-FAS-042: Частичное удаление при ошибке одного timelog
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock GET возвращает 3 timelogs, первый DELETE успешен (204), второй — ошибка (404), третий — успешен (204)
- **Когда:** Вызван `УдалитьЗаписиОВремениПоДате(ВнешняяЗадача, "2024-01-15")`
- **То:** Возвращается 2 (два удалены успешно), исключение не генерируется

---

## Задача FAS-013: Обработка «ИнтеграцияGitLab» — ПолучитьЗаписиОВремени

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `ПолучитьЗаписиОВремени(ВнешняяСистема, ДатаНач, ДатаКон, Задача)` → ТаблицаЗначений  
**Описание:** Получает timelogs за период через GET /projects/:id/time_entries.

### Юнит-тесты

#### ТЕСТ-FAS-043: Получение timelogs за период
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает массив timelogs со статусом 200
- **Когда:** Вызван `ПолучитьЗаписиОВремени(ВнешняяСистема, "2024-01-01", "2024-01-31", Задача)`
- **То:** Возвращается ТаблицаЗначений с полями timelog_id, project_name, issue_iid, issue_title, duration_seconds, spent_at, description, web_url

#### ТЕСТ-FAS-044: Формирование URL для GET time_entries
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована с БазовыйURL="https://gitlab.example.com", ВнешняяСистема имеет БазовыйURL задачи = "group/proj"
- **Когда:** Вызван `ПолучитьЗаписиОВремени(ВнешняяСистема, "2024-01-01", "2024-01-31", Задача)`
- **То:** GET отправлен на URL "https://gitlab.example.com/api/v4/projects/group%2Fproj/time_entries?date_on_or_after=2024-01-01&date_before=2024-01-31"

---

## Задача FAS-014: Обработка «ИнтеграцияGitLab» — ПолучитьЗадачи

**Модуль:** `Обработка.ИнтеграцияGitLab`  
**Метод:** `ПолучитьЗадачи(ИдентификаторПользователя)` → ТаблицаЗначений  
**Описание:** Получает список задач (Issues) пользователя через GitLab API.

### Юнит-тесты

#### ТЕСТ-FAS-045: Получение списка задач GitLab
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована, mock HTTP возвращает массив задач со статусом 200
- **Когда:** Вызван `ПолучитьЗадачи(42)`
- **То:** Возвращается ТаблицаЗначений с полями ID, IID, Title, WebURL, ProjectID

#### ТЕСТ-FAS-046: Формирование запроса задач пользователя
- **Дано:** Обработка «ИнтеграцияGitLab» инициализирована с БазовыйURL="https://gitlab.example.com"
- **Когда:** Вызван `ПолучитьЗадачи(42)`
- **То:** GET отправлен на URL "https://gitlab.example.com/api/v4/issues?assignee_id=42&state=opened"
