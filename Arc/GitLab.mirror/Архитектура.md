# Архитектура подсистемы GitLab.mirror

## 1. Общая концепция

Изолированная библиотека для работы с GitLab API из конфигурации 1С. Обеспечивает прямой просмотр и управление таймлогами (timelogs) в GitLab через Timelogs API, а также работу с задачами (Issues) и проектами.

**Принцип изоляции:** весь код работы с HTTP и GitLab API сосредоточен в этой подсистеме. Внешние потребители взаимодействуют только через публичный модуль `GitLabAPI`.

## 2. Структура подсистемы

### 2.1 Общие модули

#### Модуль «HTTPКоннектор» (готовый)
Библиотека для выполнения HTTP-запросов из 1С. Предоставляет методы:
- `ВыполнитьGET(URL, Заголовки)` — GET запрос
- `ВыполнитьPOST(URL, ТелоЗапроса, Заголовки)` — POST запрос
- `ВыполнитьDELETE(URL, Заголовки)` — DELETE запрос
- `ВыполнитьPUT(URL, ТелоЗапроса, Заголовки)` — PUT запрос

#### Модуль «GitLabAPI»
Публичный модуль для работы с GitLab API. Использует HTTPКоннектор.

**Инициализация:**
- `УстановитьБазовыйURL(БазовыйURL)` — установка базового URL GitLab сервера
- `УстановитьТокен(Токен)` — установка токена аутентификации

**Методы работы с задачами (Issues):**
- `ПолучитьНазначенныеЗадачи(ИдентификаторПользователя, Состояние)` — GET /api/v4/issues?assignee_id=...&state=opened
  - Возвращает ТаблицуЗначений: ID, IID, Title, WebURL, ProjectID

**Методы работы с таймлогами (Timelogs):**
- `ДобавитьЗатраченноеВремя(ПроектID, IssueIID, ДлительностьВСекундах, ДатаРаботы, Описание)` — POST /projects/:id/issues/:issue_iid/time_spent
- `УдалитьTimelog(ПроектID, IssueIID, TimelogID)` — DELETE /projects/:id/issues/:issue_iid/timelogs/:timelog_id
- `ПолучитьTimelogи(ПроектID, IssueIID, ДатаНач, ДатаКон)` — GET /projects/:id/issues/:issue_iid/timelogs?spent_at_on_or_after=...&spent_at_before=...
- `ПолучитьСтатистикуВремени(ПроектID, IssueIID)` — GET /projects/:id/issues/:issue_iid/time_stats

**Методы работы с проектами:**
- `ПолучитьПроекты(Состояние)` — GET /api/v4/projects?membership=true&state=active
  - Возвращает ТаблицуЗначений: ID, Name, WebURL, NamespacePath

**Методы для формы «ТаймлогиGitLab»:**
- `ПолучитьTimelogиПоСотруднику(ИдентификаторПользователя, ДатаНач, ДатаКон)` — GET /api/v4/users/{user_id}/time_entries?date_before=...&date_on_or_after=...
  - Возвращает ТаблицуЗначений: timelog_id, project_name, issue_iid, issue_title, duration_seconds, spent_at, description, web_url
- `ПолучитьTimelogиПоДатеИСотруднику(Дата, ИдентификаторПользователя)` — получение таймлогов за конкретную дату для сотрудника
- `УдалитьTimelogПоID(timelog_id, проект_идентификатор)` — удаление таймлога по его ID

### 2.2 Форма «ТаймлогиGitLab»

Назначение: прямой просмотр и управление таймлогами в GitLab.

#### Параметры отбора
- **Сотрудник** — выбор из справочника Сотрудники (обязательный)
- **Дата** — конкретная дата или период начала/конец (обязательный)

#### Таблица результатов
| Колонка | Описание |
|---------|----------|
| ID записи | timelog_id из GitLab |
| Проект | название проекта |
| Задача | issue iid + название задачи |
| Дата/время начала | spent_at |
| Длительность | секунды (с форматированием в часы/минуты) |
| Описание | summary |
| Веб-ссылка | ссылка на задачу в GitLab |

#### Действия
- **Удалить выбранную запись** — DELETE через API с подтверждением
- **Открыть веб-страницу задачи** — переход по ссылке на Issue в GitLab
- **Экспорт в Excel/MXL** — выгрузка результатов в табличный документ

## 3. Настройки подключения

### 3.1 Константы подсистемы
- **GitLABБазовыйURL** — адрес GitLab сервера (строка)
- **GitLABТокенДоступа** — токен аутентификации (строка)

### 3.2 Регистр сведений «НастройкиПользователейGitLab» (опционально)
Для персонализированных токенов:
- **Измерения**: Сотрудник
- **Ресурс**: ТокенДоступа

## 4. Расширяемость на другие системы

Архитектура позволяет легко добавлять новые внешние системы:

1. Новое значение в перечисление «ТипВнешнихСистем».
2. Новый модуль API (например, `JiraAPI`) с методами работы с системой.
3. Регистрация в модуле диспетчеризации.
4. Настройка подключения в константах или регистре сведений.

## 5. Схема взаимодействия подсистемы

```
┌─────────────────────────────────────────────────────────────┐
│              Внешние потребители (любая подсистема)         │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Форма «ТаймлогиGitLab»                               │  │
│  │  - Поиск по дате и сотруднику                        │  │
│  │  - Удаление/редактирование                           │  │
│  │  - Экспорт в Excel                                   │  │
│  └──────────────────────┬────────────────────────────────┘  │
│                         │ вызывает                          │
│  ┌──────────────────────▼────────────────────────────────┐  │
│  │           GitLabAPI (публичный модуль)                │  │
│  │  - ДобавитьЗатраченноеВремя() → POST timelogs         │  │
│  │  - УдалитьTimelog() → DELETE timelogs                 │  │
│  │  - ПолучитьTimelogи() → GET timelogs                  │  │
│  │  - ПолучитьНазначенныеЗадачи() → GET issues           │  │
│  │  - ПолучитьTimelogиПоСотруднику()                     │  │
│  └──────────────────────┬────────────────────────────────┘  │
└─────────────────────────┼───────────────────────────────────┘
                          │ использует
┌─────────────────────────▼───────────────────────────────────┐
│              GitLab.mirror                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  HTTPКоннектор (готовый)                              │  │
│  │  - ВыполнитьGET()                                     │  │
│  │  - ВыполнитьPOST()                                    │  │
│  │  - ВыполнитьDELETE()                                  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  Настройки подключения:                                     │
│  - Константы или регистр сведений                          │
│  - Токен аутентификации                                    │
└─────────────────────────────────────────────────────────────┘
```

## 6. API эндпоинты GitLab

### 6.1 Issues (Задачи)
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v4/issues?assignee_id={id}&state=opened` | Получить назначенные задачи |
| GET | `/api/v4/projects/{id}` | Получить информацию о проекте |

### 6.2 Timelogs (Записи времени)
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/projects/{id}/issues/{iid}/time_spent` | Добавить затраченное время |
| DELETE | `/projects/{id}/issues/{iid}/timelogs/{timelog_id}` | Удалить запись времени |
| GET | `/projects/{id}/issues/{iid}/timelogs?spent_at_on_or_after={date}&spent_at_before={date}` | Получить таймлоги за период |
| GET | `/projects/{id}/issues/{iid}/time_stats` | Получить статистику времени |
| GET | `/api/v4/users/{user_id}/time_entries?date_on_or_after={date}&date_before={date}` | Получить таймлоги сотрудника |

### 6.3 Формат запроса добавления времени (POST)
```json
{
    "duration": "1h30m",
    "spent_at": "2024-01-15T10:00:00Z",
    "summary": "Описание выполненной работы"
}
```

### 6.4 Формат ответа (содержимое time_stats)
```json
{
    "time_spent": 5400,
    "total_time_spent": 14400
}
```

## 7. Безопасность

- Токен аутентификации хранится в константе или регистре сведений.
- HTTP-запросы выполняются по HTTPS.
- При ошибке аутентификации — токен не сбрасывается, предлагается обновить.
- Логирование успешных и неудачных вызовов API (опционально).